#include <iostream>
#include <vector>
#include <cmath>
#include <random>
using namespace std;

int main() {
    int n, choice;
    vector<double> arr, filtered;
    cout << "1 — Ручной ввод, 2 — Случайные числа. Выберите: ";
    cin >> choice;